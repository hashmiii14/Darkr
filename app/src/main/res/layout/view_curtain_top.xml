<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/curtainTopRoot"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical">

    <!-- Top Dark Mask Body -->
    <View
        android:id="@+id/maskTopBody"
        android:layout_width="match_parent"
        android:layout_height="220dp"
        android:background="@color/mask_dark_95" />

    <!-- Slit Drag Header / Control Bar -->
    <LinearLayout
        android:id="@+id/slitDragHandle"
        android:layout_width="match_parent"
        android:layout_height="32dp"
        android:background="@color/white_pure"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:paddingHorizontal="14dp">

        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="@string/curtain_drag_hint"
            android:textColor="@color/black_true"
            android:textSize="10sp"
            android:textStyle="bold"
            android:letterSpacing="0.08" />

        <ImageView
            android:id="@+id/btnCloseCurtain"
            android:layout_width="22dp"
            android:layout_height="22dp"
            android:src="@drawable/ic_panic"
            android:padding="2dp"
            android:contentDescription="@string/action_close" />
    </LinearLayout>

</LinearLayout>
